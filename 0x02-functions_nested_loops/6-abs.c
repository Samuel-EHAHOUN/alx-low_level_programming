/**
 * _abs - Entry point
 *
 * Description: Check if n is lowercase
 *
 * @n: is the character to test
 *
 * Return: return n
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
